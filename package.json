{
  "name": "next-anim-lib-comparison",
  "private": true,
  "version": "1.0.0",
  "workspaces": {
    "packages": [
      "default/**"
    ]
  },
  "scripts": {
    "dev:default": "yarn default dev",
    "default": "yarn workspace default"
  },
  "volta": {
    "node": "18.16.0",
    "yarn": "1.22.10"
  }
}
