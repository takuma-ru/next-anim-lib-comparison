{
  "name": "next-anim-lib-comparison",
  "private": true,
  "version": "1.0.0",
  "workspaces": {
    "packages": [
      "default/**",
      "framer-motion/**",
      "react-spring/**"
    ]
  },
  "scripts": {
    "dev:default": "yarn default dev",
    "dev:framer": "yarn framer dev",
    "dev:spring": "yarn spring dev",
    "default": "yarn workspace default",
    "framer": "yarn workspace framer-motion",
    "spring": "yarn workspace react-spring"
  },
  "volta": {
    "node": "18.16.0",
    "yarn": "1.22.10"
  }
}
